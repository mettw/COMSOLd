TODO list
=========

* Which files should be general in nature (common for all users) but aren't?
* COMSOLd should not crash if a user's completed/ directory is missing - it should create it if allowed instead.
